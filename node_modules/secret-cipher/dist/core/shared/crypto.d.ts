import { resolveCipherRuntime } from '../../runtimes';
export type CipherPayload = {
    readonly salt: Uint8Array;
    readonly iv: Uint8Array;
    readonly ciphertext: Uint8Array;
};
export declare const parseCipherPayload: (cipher: string) => CipherPayload;
export declare const encodeCipherPayload: ({ salt, iv, ciphertext, }: CipherPayload) => string;
export declare const deriveKey: ({ info, salt, secret, runtime, }: {
    readonly info: Uint8Array;
    readonly salt: Uint8Array;
    readonly secret: Uint8Array;
    readonly runtime: Awaited<ReturnType<typeof resolveCipherRuntime>>;
}) => Promise<Uint8Array>;
